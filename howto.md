アプリケーション概要

本リポジトリには、散歩用のルートやスポットを地図上で表示し、利用者の歩行ログや投稿を扱うWebアプリのコードが含まれています。
構想の詳細は「仕様の整理.md」にまとめられており、アプリの主な機能例は下記の通りです。

基本コンセプト
自治体が整備した散歩ルートを市民が歩き、訪問記録・体験ログを残して共有できる

まるで御朱印のように「歩いた証」を可視化する仕組みを目指す

開発フェーズと主な機能
フェーズ1（自治体主導）

ルート定義（始点・終点・経由スポット、距離やテーマ）とスポット登録を行い、紙のPDFマップとWeb地図で公開

フェーズ2（市民参加）

写真投稿、コメント、タグ追加、評価、追加スポット提案、派生ルート投稿など協働コンテンツが可能

歩行ログや訪問済みスポットの記録、マイページでの歩行履歴管理、バッジ・スタンプ獲得などの機能

フェーズ3（コミュニティ化）

バッジ制度やスタンプラリー、季節イベント、ポイント制といったゲーミフィケーション要素を導入

アプリとしての主機能と技術案
地図上での散歩ルート表示、歩行開始/終了の記録、GPSログ取得、スポット到達判定などを提供

ログ保存方法やシェア機能、バッジ表示、写真アップロードなどを想定

画面UI（実装例）
index.html では地図表示に加えて、暗渠・道路・ルート・スポットなど各レイヤーの表示切替や、ルート選択プルダウン、道路属性フィルターを配置

src/main.ts ではGeoJSONデータの読み込み・レイヤー管理・フィルタリング処理・現在地表示などのロジックを実装

補助スクリプト
generate-route-manifest.js：data/route 配下のルートファイルから public/route-list.json を生成

preprocess_intersections.py：GeoJSONの道路データから交差点（分岐点）を抽出するPythonスクリプト

このように、自治体が提供する散歩ルートを中心に、市民参加型の投稿・記録機能やゲーミフィケーション要素を組み合わせたアプリを想定しています。地図表示にはLeafletを使用し、GeoJSON形式でルートやスポットを管理する構成です。既存のWeb地図上で簡単にルートを追加・公開できるため、観光や地域活性化に活用しやすい点が特徴です。

# 新規ルートデータの追加方法

1.  新しいルートデータファイル（`.json`または`.geojson`形式）を`data/route`ディレクトリに追加します。
2.  ターミナルで以下のコマンドを実行して、ルートリストを更新します。

    ```bash
    npm run build
    ```

これにより`public/route-list.json`が再生成され、新しいルートがアプリケーションで利用可能になります。